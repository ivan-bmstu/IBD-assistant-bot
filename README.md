# Poop Tracker Bot 🤖

Telegram‑бот для людей с болезнью Крона и язвенным колитом. Помогает фиксировать факты походов в туалет, консистенцию стула и заметки, чтобы отслеживать состояние и динамику.

## 🎯 Цели проекта
- Упростить ведение дневника симптомов
- Дать удобный интерфейс для регулярной фиксации записей
- Сохранить минимальный набор данных без персональных сведений

## ✨ Возможности
- Запись факта похода в туалет
- Выбор консистенции стула (жидкий / кашицеобразный / нормальный / твёрдый)
- Добавление заметки к записи (опционально)
- Настройка таймзоны пользователя
- Команды /start, /help, /about

## 📦 Что хранится
- Пользователь: Telegram ID, язык, таймзона
- Записи: дата/время, консистенция, заметки

## 🏗️ Архитектура
```
poop-app/
├── bot/                    # Обработчики и клавиатуры Telegram
├── config/                 # Конфигурация
├── database/               # Модели и доступ к БД
├── migrations/             # Alembic миграции
├── service/                # Сервисный слой
├── tests/                  # Тесты
└── docker-compose.yml      # Docker конфигурация
```

## 🔧 Технологии
- Python 3.11+
- aiogram 3.x
- SQLAlchemy 2.0 (async)
- PostgreSQL
- Docker / Docker Compose

## 🚀 Быстрый старт

### 1. Клонирование
```bash
git clone <repository-url>
cd poop-app
cp .env.example .env
```

### 2. Настройка переменных окружения
Отредактируйте `.env` файл:
```env
BOT_TOKEN=your_bot_token_here
DB_HOST=localhost  # Если бот запускается в docker compose, укажите postgres
DB_PORT=5432
DB_NAME=poop_tracker
DB_USER=postgres
DB_PASSWORD=postgres
LOG_LEVEL=INFO
ADMIN_IDS=          # Необязательно, через запятую
```

### 3. Запуск с Docker (рекомендуется)
```bash
# Сборка и запуск всех сервисов (бот + PostgreSQL)
docker compose up -d --build

# Просмотр логов бота
docker compose logs -f bot
```

### 4. Запуск локально (без Docker)
```bash
# Установка зависимостей
poetry install

# Запуск бота
poetry run python -m bot.main
```

Требуется доступный PostgreSQL и корректные `DB_*` переменные.

## 🐳 Docker развертывание

### Полное развертывание
```bash
docker compose up -d --build
docker compose logs -f bot
```

### Только база данных
```bash
docker compose up -d postgres
```

### pgAdmin (опционально)
Сервис pgAdmin уже включён в compose и запускается вместе с базой. Доступ:
- http://localhost:5050
- логин/пароль из `.env` (`PGADMIN_DEFAULT_EMAIL`, `PGADMIN_DEFAULT_PASSWORD`)

## 📖 Использование бота

### Команды
- `/start` — начать работу (первый запуск включает настройку таймзоны)
- `/help` — справка
- `/about` — информация о проекте

### Основной сценарий
1. Нажмите кнопку **📝 Сделать запись**
2. Выберите консистенцию
3. Добавьте заметку или пропустите

## 🗄️ Миграции
Для управления схемой используется Alembic:
```bash
alembic upgrade head
alembic revision --autogenerate -m "Описание изменений"
```

## 🔮 Потенциальные фичи
- История записей и фильтрация по датам
- Статистика по периодам (частота, распределение консистенции)
- Экспорт данных (CSV / JSON)
- Напоминания и регулярные уведомления
- Теги и триггеры (еда, лекарства, стресс)
- ИИ‑анализ данных и поиск корреляций
- Личный кабинет или веб‑дашборд
- Резервное копирование и восстановление данных

## ⚠️ Дисклеймер
Бот не заменяет консультацию врача. Все решения о лечении должны приниматься вместе со специалистом.
