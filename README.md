# Poop Tracker Bot 🤖

Telegram бот для отслеживания симптомов болезни Крона и язвенного колита. Помогает пользователям записывать факт похода в туалет и заметки.

## 🎯 Цели проекта

1. **Упростить ведение дневника** для людей с ВЗК (воспалительными заболеваниями кишечника) - отслеживание факта похода в туалет

## ✨ Функционал

### 📝 Запись данных
- **Походы в туалет**: факт похода в туалет и дополнительные заметки

## 🏗️ Архитектура

### Структура проекта
```
poop-app/
├── bot/                    # Основной код бота
│   ├── handlers/          # Обработчики команд
│   ├── keyboards/         # Клавиатуры
│   └── middlewares/       # Промежуточное ПО
├── database/              # Работа с базой данных
│   ├── models.py          # Модели SQLAlchemy
│   ├── crud.py            # CRUD операции
│   └── session.py         # Сессии БД
├── config/                # Конфигурация
├── tests/                 # Тесты
└── docker-compose.yml     # Docker конфигурация
```

### Технологии
- **Python 3.11+**
- **aiogram 3.x** - фреймворк для Telegram ботов
- **SQLAlchemy 2.0** - ORM для работы с базой данных
- **PostgreSQL** / **SQLite** - системы хранения данных
- **Docker** - контейнеризация

## 🚀 Быстрый старт

### 1. Клонирование и настройка
```bash
git clone <repository-url>
cd poop-app
cp .env.example .env
```

### 2. Установка Poetry (рекомендуется)

Если у вас не установлен Poetry, установите его:
```bash
# Официальный способ установки
curl -sSL https://install.python-poetry.org | python3 -
```

Или используйте pip:
```bash
pip install poetry
```

После установки убедитесь, что Poetry добавлен в PATH.

### 3. Настройка переменных окружения
Отредактируйте `.env` файл:
```env
BOT_TOKEN=your_bot_token_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=poop_tracker
DB_USER=postgres
DB_PASSWORD=postgres
USE_SQLITE=False  # Использовать SQLite для разработки
```

### 4. Запуск с Docker (рекомендуется)
```bash
# Запуск PostgreSQL
docker-compose up -d postgres

# Установка зависимостей и запуск бота
poetry install
poetry run python -m bot.main
```

### 5. Запуск без Docker
```bash
# Установка зависимостей
poetry install

# Использование SQLite (без PostgreSQL)
# Установите USE_SQLITE=True в .env

# Запуск бота
poetry run python -m bot.main
```

## 🐳 Docker развертывание

### Полное развертывание
```bash
# Сборка и запуск всех сервисов
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f bot
```

### Только база данных
```bash
docker-compose up -d postgres
```

## 📖 Использование бота

### Основные команды
- `/start` - Начать работу с ботом
- `/help` - Получить справку
- `/about` - Информация о боте

### Основной интерфейс
Бот использует кнопки для навигации:
- 📝 **Записать стул** - запись факта похода в туалет и заметок

## 🗄️ База данных

### Структура базы данных
```sql
-- Основные таблицы:
users                # Пользователи
bowel_movements      # Походы в туалет (факт похода и заметки)
```

### Миграции
Для управления миграциями используется Alembic:
```bash
# Инициализация миграций
alembic init migrations

# Создание новой миграции
alembic revision --autogenerate -m "Описание изменений"

# Применение миграций
alembic upgrade head
```

## 🧪 Тестирование
```bash
# Установка тестовых зависимостей
poetry install

# Запуск тестов
pytest tests/
```

## 🔧 Разработка

### Настройка среды разработки
1. Установите Python 3.11+
2. Установите Poetry (рекомендуется) или используйте pip
3. Настройте pre-commit хуки:
   ```bash
   pre-commit install
   ```

### Кодстайл
- **Black** - форматирование кода
- **isort** - сортировка импортов
- **mypy** - проверка типов

```bash
# Автоматическое форматирование
black .
isort .
```

## 📈 Планы развития

### Ближайшие задачи
- [ ] Генерация графиков и диаграмм
- [ ] Напоминания о записи данных
- [ ] Экспорт данных в CSV/Excel
- [ ] Мультиязычность
- [ ] Веб-интерфейс для врачей

### Долгосрочные цели
- [ ] Интеграция с медицинскими системами
- [ ] Машинное обучение для предсказания обострений
- [ ] Мобильное приложение
- [ ] Сообщество пользователей

## ⚠️ Важное замечание

**Этот бот не заменяет медицинскую консультацию!** Все решения о лечении должны приниматься под наблюдением квалифицированного врача. Бот предназначен только для отслеживания симптомов и помощи в ведении дневника.

## 📄 Лицензия

MIT License

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Контакты

Для вопросов и предложений:
- Telegram: @your_username
- Email: your.email@example.com
- Issues: [GitHub Issues](https://github.com/yourusername/poop-tracker-bot/issues)

---

*Проект создан с заботой о людях с болезнью Крона и язвенным колитом.* 💙